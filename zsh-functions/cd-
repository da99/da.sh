
if test -e "$HOME/tmp/last.cd" ; then

  local +x _last_cd="$(cat "$HOME/tmp/last.cd")"
  if test -e "$_last_cd" ; then
    builtin cd "$_last_cd"
  else
    echo "$_last_cd no longer exists." >&2
  fi

else
  echo "No previous directory." >&2
fi
