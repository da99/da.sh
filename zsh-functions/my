#/usr/bin/env zsh

dir="$1"
if test -d "/apps/$dir"; then
  cd "/apps/$dir"
else
  if test -d "/apps/my_${dir}"; then
    cd "/apps/my_${dir}"
    return 0
  else
    echo "!!! Directory not found: /apps/$dir" >&2
  fi
fi


